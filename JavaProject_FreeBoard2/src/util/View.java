package util;

public enum View {
	MAIN,					// 기본화면
	USER_JOIN,				// 회원가입(Create)
	USER_LOGIN,				// 로그인(Read)
	USER_LIST,				// 유저 리스트 출력?(Read)
	USER_UPDATE,			// 유저 정보 변경(Update)
	USER_DEL				// 유저 삭제(Delete)
}
